# Jimeng-Plugin

[![Chrome Extension](https://img.shields.io/badge/Chrome-Extension-green.svg)]()

一键批量导出即梦 AI 收藏的图片、提示词，支持导出 ZIP 素材包（图片 + 提示词）或 CSV 格式（仅提示词）。

## 安装

```bash
npm install && npm run build
```

然后在 Chrome 中加载 `dist/` 目录。